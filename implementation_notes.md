# Array

  - originally classes extended Array. this turned out not to work with IE, so the few methods that were required were reimplemented within each class

# Strings

  - IE doesn't support `[]` for strings, so `charAt` is used instead
