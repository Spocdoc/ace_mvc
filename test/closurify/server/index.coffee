closurify = lib 'index'

describe 'closurify', ->
  it 'should produce expected output when reading from a string'

    # closurify foo, release: false, (err, debug, release) ->
    #   console.log debug

    # closurify ['./foo'], release: false, (err, debug, release) ->
    #   console.log debug



